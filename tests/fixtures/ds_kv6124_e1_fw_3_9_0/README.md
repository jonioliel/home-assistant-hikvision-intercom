No real-device evidence has been supplied yet. Do not put synthetic fixtures here.

After the owner returns the Phase 0 output, review its sanitization, retain request
method/path, HTTP status, media type, namespaces and normalized response structure,
and document physical observations separately. Only then add regression tests
and capability mappings based on these captures.
