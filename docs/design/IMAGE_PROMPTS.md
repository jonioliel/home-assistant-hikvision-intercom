# Alternate interface image-generation record

Mode: built-in ImageGen (`ui-mockup`). Scope: design concepts only, fictional data, no runtime implementation.

The owner's four-screen reference was visually inspected in the conversation. Direct reference-file ingestion failed because the Windows filesystem sandbox helper could not read the file. The subsequent attempts use text-only descriptions derived from that inspection; the source image was not copied into the public repository.

## Desktop board prompt (text-only generation)

```text
Create a high-fidelity UI DESIGN PROPOSAL, a single large landscape presentation board containing FOUR equally sized desktop application screens in a clean 2x2 layout. Generate a new original four-screen UI mockup. The user's visual reference has been inspected and its design is described fully below; use this detailed specification to reconstruct its visual direction. Design a refined alternative skin for the existing Home Assistant Hikvision Intercom Manager. This is a conceptual mockup, NOT a real working product screenshot.
Match the reference closely: slim charcoal/navy left navigation rail, very light cool gray workspace, white thin-bordered panels, saturated azure blue primary buttons, gentle compact radii (8-12px), subtle shadows, restrained green/amber/red status chips, well aligned dense tables, camera thumbnails prominent, neat Hebrew typography. Avoid oversized dashboard cards, huge white gaps, decorative gradients, glass effects, marketing headings, 3D perspective and outer device frames. Screens must look like usable professional desktop software, flat front-on. Use crisp correctly shaped Hebrew, real RTL content alignment, Latin identifiers isolated LTR. Small icons consistent outlined style. All people, dates, station names and camera photos fictional.
Global navigation belongs to the INTEGRATION, headed by a small home/intercom icon and “ניהול אינטרקומים”; no unrelated Energy/Map HA destinations. Items: “סקירה”, “משתמשים”, “אירועים”, “תחנות”, “סנכרון”, “יומן שינויים”; “כלים מתקדמים” disclosure and “מראה הממשק” near bottom. Dark rail is always on the left as in supplied reference; content is Hebrew RTL.
Top-right compact design choice on all four screens, exact text “עיצוב: קיים | חדש” with “חדש” selected. This is distinct from a small sun/moon icon. No PIN reveal-eye icon.

TOP LEFT — Overview. Hebrew heading “ניהול אינטרקומים”, subtitle “כל הכניסות, במקום אחד”. A compact horizontal summary with exact coherent counts “9 תחנות”, “8 מחוברות”, “1 מנותקת”, “1 מצלצלת”. Display exactly NINE station camera cards in a 3-column by 3-row grid. Each has a realistic neutral entrance/gate/lobby camera thumbnail with no people, bold station name, small “מחובר” green or “מנותק” red label, one blue button ONLY “פתיחת דלת” with lock icon. Station names: “שער ראשי”, “כניסה לבניין א”, “כניסה לבניין ב”, “חניון”, “משרדים”, “מחסן”, “כניסת שירות”, “בריכה”, “דלת אחורית”. First station visibly ringing with red outline and red “מצלצל עכשיו” pill, plus compact “צפייה בשיחה” secondary text action. Only one station offline, its thumbnail clean slate placeholder “אין חיבור” and disabled gray open button. Other open buttons stay active. Under card a tiny separate sync chip “מסונכרן” or “ממתין לסנכרון” distinct from connection availability. Do not display IP addresses as primary content. Camera aspect ratio wide. All nine cards comfortably fit the panel.

TOP RIGHT — Users. Heading “ניהול משתמשים”. Primary blue “הוספת משתמש”, secondary “ייבוא CSV” and “סנכרון” controls. Large full-width search “חיפוש לפי שם או מזהה עובד”, compact filter chips “הכל 28”, “פעילים 25”, “לא פעילים 3”. Dense six-row data table aligned RTL: “שם”, “מזהה עובד”, “PIN”, “כרטיסים”, “תחנות”, “פעיל”, “סנכרון”, “פעולות”. Fictional residents “אור לוי”, “דנה כהן”, “נועם ברק”, “עדי ישראלי”, “צוות תחזוקה”, “אורח זמני”. Monogram circular avatars, IDs 1001 through 1006, PIN green checked icon or dash ONLY; card counts 1/2/0, assigned station fractions 5/9 or 3/9. Sync chips green “מסונכרן”, amber “ממתין”, one red “שגיאה” with link “פרטים”. Quiet edit and more icons; deletion inside more menu, not prominent. Selection checkboxes in rows. Footer results count and pagination. No personal card numbers or PIN values.

BOTTOM LEFT — Edit user. Heading “עריכת משתמש”, breadcrumb “משתמשים / אור לוי”. Two balanced columns within white panels. General details form has monogram avatar (no upload-photo feature), name “אור לוי”, employee ID 1001 read-only appearance, active toggle. Validity radio/segmented options “ללא הגבלה” selected and “תוקף מוגבל”; small explicit “אזור זמן: לפי התחנה”. PIN panel displays “קוד מוגדר” status, “החלפת קוד” and subtle “הסרת קוד”; NO dots pretending to represent a readable stored PIN. Cards panel “כרטיסי גישה”: two masked cards “•••• 4821”, “•••• 7394”, labels “ראשי”, “גיבוי”, small remove action, “הוספת כרטיס” button; no NFC phone claim. Permissions column heading “הרשאות לתחנות”, 9-row station list with single “גישה לדלת” checkbox per station. Explicitly ONLY ONE ACTIVE RELAY per station, no Door 2 column. Sticky bottom actions “ביטול”, “שמירה”, blue “שמירה וסנכרון”. Small note “הסנכרון מתבצע בנפרד לכל תחנה”.

BOTTOM RIGHT — Station. Breadcrumb “תחנות / שער ראשי”, title “הגדרות תחנה”. Left white identity panel with small intercom product silhouette, name “שער ראשי”, model DS-KV6124-E1, firmware “3.9.0”, green “מחובר”, secondary “בדיקת חיבור”. Do not invent serial number or actual device address. Right larger white section “הדלת הפעילה”: one row only, “דלת ראשית”, “ממסר 1”, “מזהה API: 1”, secondary outlined “בדיקת פתיחה”; no relay2. Underneath 3 small factual tiles “12 משתמשים”, “0 ממתינים”, “מצלמה זמינה”. Expandable rows “שעון ותצוגת זמן”, “אירועים אחרונים”, “כלים מתקדמים”. Upper right blue “סנכרון עכשיו”, lighter “רענון נתונים”. Small conceptual data only.

Board footer outside screens: “כיוון עיצוב חדש · הדמיה לאישור · נתוני דוגמה”. Maintain visual hierarchy like the reference, compact usable spacing, legibility rather than tiny decorative text. Four screens consistent visual language, high-resolution detailed UI board.
```

## Mobile, dark mode and appearance selector prompt

```text
undefined
```
