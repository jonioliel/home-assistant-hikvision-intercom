Create a precise, polished UI design proposal for a Hebrew RTL Home Assistant intercom dashboard. The application's current appearance is described below from the screenshot supplied by the owner; generate this proposal from the description. This is a design mockup for approval, not a deployed interface. Make ONE presentation board with a large desktop view above and a small mobile view inset below/right, on a quiet neutral background. Crisp readable Hebrew typography and faithful restrained white/blue design.

KEY CHANGE: remove the four giant full-width metric cards and their entire dedicated tall row. Replace them with FOUR VERY COMPACT status chips, approximately 30–34px high, all on THE SAME HORIZONTAL ROW as the desktop heading "דלתות ומצלמות". The heading stays at the far right. Chips occupy a short group to the left, with ample empty space between title and chips. Each chip contains a small simple icon, a bold number and a short text label on a SINGLE LINE: "4/4 מחוברות" (green dot), "0 מצלצלים" (bell), "3 משתמשים" (people), "0 לסנכרון" (sync). Small outlined pills, 12–13px text, 8px corner radius, very light tint, NO giant numbers and NO stacked label below a number. Preserve true LTR order within 4/4. Keep the small existing subtitle "צפייה בכניסות ופתיחת הדלתות המחוברות." directly below the heading, then a tight 16–20px gap and the camera grid. The entire desktop title-and-status region should feel approximately 70px high, not 200px.

DESKTOP: broad 1500px-style dashboard crop, heading row with chips above four equal station cards. Off-white blue-gray #F3F6FA background, white station cards, fine blue-gray borders, saturated blue #086BDB primary buttons. Four example station cards, Hebrew names "כניסה ראשית", "לובי", "משרד", "כניסה צדדית". Camera thumbnails of neutral building entrances and courtyards, small green "מחובר" badges, overlay "צפייה במצלמה", ONE blue "פתיחת דלת" button per station, distinct small "מסונכרן" pill and "ממתין" call-state label. Keep these cards consistent with the reference. No extra relays or new functions. The camera grid should visibly start much higher thanks to eliminating the metrics row.

MOBILE INSET: phone-width crop, same title, subtitle, then the same four tiny chips wrapped as two compact pairs in no more than two lines. A single station card begins immediately below. Do not force the heading and all chips onto one cramped line. Keep mobile text readable and avoid horizontal scrolling. No enormous device frame.

Presentation captions outside the app views: "מחשב — חיוויים בשורת הכותרת" and "נייד — תגיות קומפקטיות". A small discreet "הדמיה לאישור" label outside the app. All other areas should match the current simple application styling. Do not show a before view, do not show the removed large cards, do not add decorative analytics/charts. The clear focal point is the dramatic reduction of header height.