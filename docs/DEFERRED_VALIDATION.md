# יומן המשך לבדיקות — 0.19.0-alpha.1

עודכן ב־2026-09-09 לבקשת בעל הפרויקט: לקדם תוכנה לפי המפרט ומסמכי היצרן בזמן שאין
אפשרות לבדיקה פיזית, ולשמור את נקודת ההמשך. ב־0.15 נוספו קריאות Search לתוכניות שעות ובדיקת התאמת טיוטה;
לא הופעל איסוף כרטיסים ולא נשלחה פקודת שינוי הרשאות או פתיחה.
״ממומש״ ו״עבר בדיקה אוטומטית״ אינם שקולים ל״אומת פיזית״.

## הראיות השמורות

- מקור ההחלטות: [Master Spec v1.2](../CODEX_MASTER_SPEC.md), ללא שינוי במקור.
- שבעת מסמכי היצרן: [סקירת החוזים ומספרי העמודים](MANUFACTURER_PROTOCOL.md)
  ו־[טביעות קובצי המקור](vendor_sources.json). המקורות המלאים והמידע הפרטי אינם ב־GitHub.
- המכשיר שנבדק: DS-KV6124-E1, קושחה V3.9.0 build 260115. אין בכך אישור לדגמי HIK אחרים.
- [ראיות התחנה השנייה](../tests/fixtures/ds_kv6124_e1_fw_3_9_0_station_b/README.md)
  ו־[דוח הבדיקות](VALIDATION.md) מכילים תוצאות מסוננות שניתן לפרסם.
- פרטי הגישה, מזהה משתמש הבדיקה הזמני והיסטוריית פעולות ה־PIN נשמרים מקומית בתיקיית
  `private/` וביומן `.tools/live_test_resume.md`, המוחרגים מ־Git. אין להעלות אותם או את
  קובצי האבחון הגולמיים. ערכי הקודים אינם משוכפלים למסמך זה.

## בדיקות החובה הממתינות

| מזהה | מה ידוע ומה ממומש | מה לבצע כשהציוד זמין | מה ייחשב הצלחה |
| --- | --- | --- | --- |
| HW-PIN | PIN ראשוני התקבל. אחרי שינוי, הישן והחדש נדחו. נשמרו ack וקריאה חוזרת; ניסיון RightPlan מאוחר יותר לא אומת במקשים. קודי האירוע מפורשים לפי טבלת היצרן. | לקרוא מצב ומגבלות בלי ניסיון קוד; לבדוק חשד לנעילת ניסיונות. לתאם ניסיון יחיד בקוד המוגדר, ורק לפי תוצאות להמשיך לשינוי/הסרה. | החדש מתקבל, הישן נדחה, הסרה גורמת לדחייה; התאמה לאירועים ולמצב הסנכרון. בסיום מחיקה ממוקדת של משתמש הבדיקה ואימות היעדרו. |
| HW-CARD | כרטיס מקורי התקבל פיזית. CRUD וריבוי כרטיסים קיימים עם בדיקות תוכנה ואימות קריאה חוזרת. | ליצור משתמש/כרטיסי בדיקה ייעודיים, להוסיף יותר מכרטיס אחד במסגרת המגבלה, לבדוק קבלה, להסיר אחד ולבדוק דחייה. | הכרטיס שנותר עובד; המוסר נדחה; פרטי הבעלים/הכרטיס המקורי לא השתנו. לא לבצע מחיקה ריקה או גורפת. |
| HW-VALIDITY | משתמש קבוע נוצר/עודכן/נמחק מול המכשיר. תוקף מוגבל החזיר סתירה בין timeType לבין החותמות; קיימת חסימת validity_timezone_mismatch. | לתעד שעה/אזור זמן של HA והמכשיר, לקבוע חלון קצר למשתמש בדיקה, לקרוא חזרה ולבדוק לפני/בתוך/אחרי החלון. | דחייה לפני ואחרי וקבלה בתוך החלון; פרשנות עקבית של אזור הזמן. לא להסיר חסימה על בסיס ack בלבד. |
| HW-RING | קריאת מצבים ואירועים קיימת. הניסיון הקודם השמיע תפוס וכל 283 הדגימות היו idle. | להכין יעד חיוג מתאים, להפעיל הקלטת מצב/זרם מסוננת, ללחוץ בתיאום ולחזור על הרצף. | ringing ואירוע HA פעם אחת לכל לחיצה, הבלטת מצלמת התחנה וחזרה למצב המתאים; אין אירוע חדש מהיסטוריה. |
| HW-VIDEO | snapshot ו־RTSP פוענחו; ישות HA והנגן נבדקו בתוכנה. | לפתוח וידאו חי בישות HA ובממשק, במחשב ובנייד, ואז לבחון מצלמה בזמן צלצול. | וידאו חי יציב בשני המקומות, ללא סיסמאות בכתובות הדפדפן, התאוששות לאחר סגירה/פתיחה. |
| HW-FLEET | תשע תחנות מדומות, תורים עצמאיים ופתיחות מקבילות נבדקו. בדיקת דקה אחת בתחנה פיזית עברה. | להפעיל תשע תחנות אמיתיות, לנתק יעד אחד, לערוך/לבטל הרשאה, להפעיל מחדש HA ולהחזיר את היעד; להריץ עומס ממושך ולתעד משך ומשאבים. | יתר התחנות פעילות בזמן התקלה, השינויים והמחיקות נשמרים ומאומתים בחיבור מחדש, בלי הכפלות או אובדן מצב. |
| HW-CSV | 500 משתמשים × תשע תחנות נבדקו במדמה; שמירה אטומית, שגיאות, ביטול, סודות ו־HA API נבדקים אוטומטית. | להתחיל בקובץ קטן של משתמשי בדיקה, לבדוק תצוגה מקדימה, ייבוא, קריאה חוזרת ויעד מנותק. | כל השורות שאושרו נשמרו; לכל תחנה סטטוס אמיתי, יעד מנותק נשאר ממתין, אין ייבוא חלקי עקב שורת קובץ שגויה. קבלה פיזית של PIN/כרטיס נמדדת בנפרד. |
| HW-ENROLL | רישום דרך הקורא ממומש ב־0.12. המכשיר מפרסם תמיכה ואורך 1–32 ללא בחירת קורא; היכולות נקראו בלי איסוף כרטיס. | משתמש וכרטיס בדיקה: קריאה וביטול, קריאה ואישור, אימות סנכרון וקבלה, הסרה ודחייה; לבדוק תפוגה והתנהגות הקורא לאחר ביטול. | בחירת קורא לפי היכולת, אין שמירה ללא אישור, הכרטיס מאומת פיזית לאחר הסנכרון, ניקוי מלא. [פרטים](CARD_ENROLLMENT.md). |
| HW-REPORT | צבירה וייצוא מסונן של כל הרשומות השמורות נבדקו, כולל יותר מעמוד אחד והפרדת אימות מפתיחה. | לבצע מספר פעולות מתואמות, לסנן לפי זמן/אדם/תחנה ולהשוות לאירועים שנשמרו ול־CSV. | הספירות והמסננים תואמים לרשומות, רשומת פתיחה אינה נספרת כאימות נוסף, השלמות היסטוריה מסומנות. |

ממסר 2 כבוי ומוחרג בפרויקט. מיפוי API door 1 כבר אומת בשתי פתיחות ובחזרה למצב הרגיל.
מענה לשיחה ממתין להתקנת מסך/יעד תומך. מצב onCall מתועד כעסוק ואינו הוכחה למענה.
לא שולחים קודים שוב ושוב כדי לעקוף נעילת ניסיונות, ולא משנים כרטיס או משתמש קיים של הבעלים.

לכל הרצה יש לרשום: מזהה הבדיקה, תאריך ושעה עם אזור זמן, גרסת האינטגרציה ו־commit,
דגם/קושחה, מזהה תחנה בדוח מסונן, מצב התחלתי, הפעולה המדויקת, תוצאת המשתמש,
תוצאת API/קריאה חוזרת/אירוע, עבר/נכשל/לא נבדק, מיקום הראיה וניקוי שבוצע.
קודים/מספרי כרטיס מלאים ופרטי גישה נשארים ביומן הפרטי בלבד. כשל יוביל לתיקון ממוקד
ולבדיקת חזרה; אין לעדכן את סעיף הקבלה ל״עבר״ רק משום שבדיקת מדמה עברה.

## תכונות Phase 6 והחלטת ההמשך

CSV ודוחות פעילות בסיסיים מומשו ב־0.11; רישום כרטיס דרך הקורא ממומש ב־0.12
וממתין לאימות פיזי. שאר תכונות ההמשך עדיין אינן מוצגות
כפעילות. פרוטוקול כללי אינו מספיק לקביעת התמיכה של הקושחה המסוימת:

| תכונה | המידע הדרוש לפני השלמה |
| --- | --- |
| שמע דו־כיווני ופקודות מענה/דחייה/סיום | יעד שיחה, יכולות ורצף סשן מוכחים; מסמכי היצרן עמודים 128–130, 486–490. חסר אימות פרוטוקול השיחה והאודיו מול הציוד. |
| go2rtc / WebRTC | בחירת פריסת שירות המדיה והרשאותיו, מסלול שמע מוכח ובדיקת לקוחות. נגן HLS ו־HA camera כבר קיימים. |
| רישום כרטיס דרך הקורא | המימוש הושלם לפי CaptureCardInfo ויכולות שנקראו בפועל; HW-ENROLL פתוח לאימות התנהגות הקורא וקבלה פיזית. |
| שעות שבועיות וחגים | ב־0.15 יש טיוטות ובדיקת התאמה עם Search מאומת. נקראו 255 תבניות, 255 שבועות, 64 קבוצות ו־300/1024 חגים. GET ישיר עדיין נכשל; קריאת חגים מלאה, בעלות, הקצאה, כתיבה ושיוך חסרים. [פרטי המימוש והמשך האימות](ACCESS_SCHEDULES.md). |
| NFC נייד / Hik-Connect | מסלול הקצאת credential נתמך וזכאות המכשיר/חשבון. התיעוד הנוכחי אינו מוכיח זאת; אין שכפול NFC או ניהול חשבון מומצא. |

המשך עבודה תוכנתית אינו סוגר את עשרת סעיפי הקבלה הפתוחים: המדידה הקבועה נשארת
[28 מתוך 38 — 73.7%](COMPLETION_HE.md). Phase 6 מחוץ למכנה; שלוש הרחבות שימושיות
נוספו בלי להציג עלייה מלאכותית באחוזי האימות הפיזי.


נקודת המשך HW-SCHEDULE: להכין טיוטה ולבדוק תצוגה מקדימה ללא כתיבה. לפני הפעלת אכיפה
יש להשלים קריאת חגים בגבולות מאומתים, לאמת בעלות/כתיבה/קריאה חוזרת ושיוך משתמש בדיקה, ואז לבדוק פיזית
חלון שבועי, חריג חג וגבולות זמן. אין כעת משאבי תזמון שנוצרו במכשיר ודורשים ניקוי.

## המשך אימות שעון — HW-CLOCK

ב־0.14 נקראו שעון התחנה, כללי DST ומצב NTP פעמיים בלי כתיבה. היסט ‎+03:00 והפרש
של אפס שניות מעוגל אומתו בקריאת הלקוח החדש. יש להשוות אירוע אמיתי בין תצוגת המכשיר,
מסך האירועים וה־CSV; לבדוק את ברירת המחדל ואת האפשרות הידנית, ובהמשך את מעבר השעון
בציוד. אין לשנות את שעון ציוד הגישה הפעיל כדי להאיץ את הבדיקה. כללי DST הנוכחיים
כוללים אפריל/אוקטובר ואינם הוכחה שהוגדר אזור IANA ישראל. [פרטים](TIME_ZONES.md).
HW-VALIDITY נשאר פתוח: תיקון תצוגה אינו מאמת קבלת קוד/כרטיס בחלון הרשאה.


## ראיית תזמון חדשה — HW-SCHEDULE, גרסה 0.15

[החיפוש המאומת](SCHEDULE_INVENTORY.md) אינו קובע שהמזהים פנויים: קבוצת חג אחת פעילה,
255 תוכניות שבועיות מופיעות בהפניות גם מתבניות כבויות, ובעלות ושיוכי משתמשים לא נסרקו.
לא נוספו משאבי בדיקה או פעולות ניקוי. ראיות Search פרטיות נשמרו מקומית; הדגימה הציבורית
מסוננת. פרשנות חלונות הקושחה והקבלה הפיזית נשארות לבדיקה לאחר השלמת מנגנון כתיבה.


## נקודת המשך להשוואת תוכניות — 0.16

שתי קריאות ייצור עם שמירת נקודת ייחוס מקומית וטעינתה מחדש ביניהן הראו רשומות זהות,
תוך שמירת הסימון החלקי לחגים. לא נוצרו משאבי תזמון במכשיר ואין ניקוי ציוד חדש.
במערכת המותקנת: בדיקת התאמה, שמירת נקודת ייחוס, קריאה נוספת והשוואה; אפשר לבדוק
את שרידותה אחרי אתחול HA. אין צורך לשנות תוכניות פעילות. כשלי אחסון, שינוי תוכן,
זהות ותגובות שהתיישנו נבדקו בתוכנה; אכיפה פיזית וכתיבה נשארות להמשך.
[מדריך](SCHEDULE_BASELINES.md). ראיה פרטית מקומית: `.tools/schedule-baseline-production.json`.


## 0.17 schedule workflows — installed acceptance to run

- On the installed panel, create a draft; copy days; clone with holidays; export and import a new
  copy. Confirm originals and unsaved edits survive, repeated names are distinct, and reload works.
- Run the station assessment queue; inspect one offline station and cancel remaining queued work.
  Compare report labels and partial counts with each selected station. This is not a fleet soak.
- Run the dependency audit. Empty/missing RightPlan stays unknown. Compare explicit template
  assignments when a station with known configured schedules is available; do not infer enforcement.
- Verify all reads leave user credentials, relay state and station schedules unchanged.

The 2026-09-09 production audit read three users with unknown defaults and sent zero writes.
PIN/card lifecycle, validity timezone contradiction, real bell sequence, media acceptance and
nine-station hardware/reconnect/reboot tests remain pending as documented above.


## בדיקת הצעות מקומיות במערכת המותקנת — 0.18

אפשר לבדוק מרחוק: בחירת טיוטה שמורה, השוואת מזהים מפורשים, שמירה מקומית, הפעלה מחדש
של HA, קריאה חוזרת, ייצוא ומחיקה. ודאו שהמקור מסומן כמשתנה אחרי עריכת הטיוטה, ושמזהים
חופפים באותו מכשיר נדחים. ההצעה חייבת להמשיך להציג שאין אפשרות החלה על ציוד.

נתוני השוואה מקומיים נשמרים לצד הטיוטה המקורית, כדי לאפשר בהמשך בירור שינויים.
אין לבצע כתיבת תצורות או שיוך משתמשים דרך קובץ הייצוא לצורך בדיקה לא מתואמת.
השלבים הבאים דורשים אימות בעלות ומשאבים, חוזה כתיבה/קריאה חוזרת והתאוששות, ובהמשך
משתמש בדיקה ובדיקה פיזית של חלונות שבועיים, חגים וחצות/שעון קיץ. כל שערי החובה
הקודמים נשארים פתוחים; הבדיקות החדשות לא נחשבות הוכחת אכיפה פיזית.


## נקודת המשך למנוע כתיבת תוכניות — 0.19

יומן ומנוע התאוששות ממומשים עם מתאם מדומה בלבד. הדוח הפרטי
`.tools/schedule-recovery-simulation.json` כולל תשעה תרחישים; ניתן לשחזר אותם בכל עת
לפי [המדריך](SCHEDULE_RECOVERY.md). תוצאת verified בכלי מציינת התאמה בקריאה במדמה בלבד.
לפני חיבור למכשיר יש להשלים בעלות, תלויות, מלאי רלוונטי ומתאם כתיבה מאומת, ואז לשלב
אחסון פרטי, Repairs, תור הפעלה ופתרון תוצאות לא ידועות. אין כעת תצורות חדשות בציוד
או פעולות ניקוי חדשות. בדיקות חלונות שעות, חגים ושעון נותרו לתיאום הפיזי הקודם.


## Owner-reported two-station PIN acceptance — 2026-09-09

The owner reports connecting two intercoms to the installed integration, creating a user and
synchronizing it successfully to both. After replacing the PIN, the new PIN worked and the old
PIN was rejected. This confirms owner-witnessed creation and replacement in that installation;
the exact installed integration version and both firmware versions were not provided. It does
not establish the cause of the earlier commissioning failure, PIN removal without replacement,
cleanup of the original temporary user, or nine-station acceptance. No PIN values were requested
or recorded. DoD16 remains open only for its remaining removal/lifecycle evidence; the fixed
mandatory count remains28/38 until that combined requirement is completed.
